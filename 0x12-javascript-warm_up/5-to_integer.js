#!/usr/bin/node

const arg2 = process.argv[2];
const parsedNumber = parentInt{arg2};

if (isNaN(parentNumber)) {
  console.log('Not a number');
} else {
  console.log('My number: ${parentNumber}');
}
